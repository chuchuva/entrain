<%= form_for([:admin, @installment_plan.new_record? ? @program : nil,
      @installment_plan]) do |f| %>
  <% if @installment_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@installment_plan.errors.count, "error") %> prohibited this installment plan from being saved:</h2>

      <ul>
      <% @installment_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_payment %><br>
    <%= f.text_field :first_payment %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <% if @installment_plan.new_record? %> 
    <div class="field">
      <%= f.label :coupon_id %><br>
      <%= f.collection_select :coupon_id, @program.coupons, :id, :code,
                              include_blank: '' %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
