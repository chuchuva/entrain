<p id="notice"><%= notice %></p>

<p>
  <strong>Slug:</strong>
  <%= @page.slug %>
</p>

<p>
  <strong>Title:</strong>
  <%= @page.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @page.content %>
</p>

<%= link_to 'Edit', edit_admin_program_page_path(@page.program_id, @page) %> |
<%= link_to 'Delete', [:admin, @page.program, @page],
      method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', admin_program_pages_path(@page.program_id) %>
