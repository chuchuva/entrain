<p id="notice"><%= notice %></p>

<p>
  <strong>Slug:</strong>
  <%= @page.slug %>
</p>

<p>
  <strong>Title:</strong>
  <%= @page.title %>
</p>

<h3>Content</h3>
<pre><%= @page.content %></pre>

<%= link_to 'Edit', edit_admin_page_path(@page) %> |
<%= link_to 'Delete', [:admin, @page],
      method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', admin_program_pages_path(@page.program_id) %>
